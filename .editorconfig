root = true

# 기본 설정 (모든 파일에 적용)
[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2
trim_trailing_whitespace = true

# Markdown 파일은 예외 처리 (줄 끝 공백 없애지 않음)
[*.md]
trim_trailing_whitespace = false